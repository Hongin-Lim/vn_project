{
  "name": "vn_project",
  "version": "1.0.0",
  "description": "vietnam service",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "flutter build web",
    "deploy": "firebase deploy --only hosting",
    "start": "flutter run -d chrome"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-tools": "^13.28.0"
  }
}
